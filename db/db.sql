CREATE TABLE price (
                               id INT NOT NULL,
                               item DECIMAL(20, 2) NOT NULL,
                               unitPrice DECIMAL(20, 2),
                               specialPrice DECIMAL(20, 2)

);